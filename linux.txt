pwd → 現在のディレクトリを調べる

mkdir → ディレクトリを作る

cd → ディレクトリを移動

cd .. → １つ上のディレクトリに戻る

cd ~ → ホームディレクトリに移動
  
ls → ディレクトリの中身を表示

touch → ファイルの作成

rm → ファイルの完全削除

rm -r → ディレクトリの削除

rm * → ファイルを複数削除する

mv → ファイルの移動

cp → 複製する

cat → ファイルの内容確認